# ======================================================================
# flyway.conf - Configuration file for local testing (Java Properties Format)
# ======================================================================

# --- Database Connection Settings ---
flyway.url=jdbc:oracle:thin:@//10.12.43.235:1521/orclpdb.localdomain?oracle.jdbc.restrictGetTables=false&internal_logon=sysdba
flyway.user=sys
flyway.password=pass

# --- Schema and Location Settings ---
# In properties format, locations are comma-separated without brackets
flyway.locations=filesystem:/opt/dba_deployment/sql